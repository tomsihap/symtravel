{% extends 'base.html.twig' %}

{% block body %}

    {{ form_start(form) }}

        {{ form_widget(form) }}

        <button class="btn btn-success pull-right">Envoyer</button>

    {{ form_end(form) }}

{% endblock %}
